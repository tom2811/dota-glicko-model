# Exploratory Notebooks

The Jupyter notebooks in this directory are purely historical records of our Exploratory Data Analysis (EDA). They were used to test initial assumptions, map API data, and build the first failed team-level baselines. 

**None of this code is used in the final model.** All production code was extracted and rewritten into the `src/` directory.
